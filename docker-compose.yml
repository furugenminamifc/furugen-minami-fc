services:
  web:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./:/usr/share/nginx/html:ro
  ai-server:
    build: ./ai-server
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
