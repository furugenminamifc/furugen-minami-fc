# 更新手順

1. 現在のVer.4フォルダとSupabaseデータをバックアップします。
2. GitHubの「Add file」→「Upload files」を開きます。
3. `index.html`、`assets`、`manifest.webmanifest`、`sw.js` をアップロードします。
4. Commit messageへ `Add Ver5 Phase1 AI` と入力してCommitします。
5. 2〜5分後に公開サイトを再読み込みします。
6. 古い表示の場合はSafariのタブを閉じて再度開くか、ホーム画面アプリを一度終了して開き直します。
